import "package:flutter/material.dart";

const appBarTheme = AppBarTheme(elevation: 0, scrolledUnderElevation: 0);

const bottomAppBarTheme = BottomAppBarTheme(elevation: 0);

const cardTheme = CardTheme(margin: EdgeInsets.zero);

const tabBarTheme = TabBarTheme(labelPadding: EdgeInsets.all(4));
